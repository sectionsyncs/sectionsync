<!DOCTYPE html>
<html lang="en" class="dark">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Choose Subscription Plan</title>
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  <link href="https://cdn.jsdelivr.net/npm/flowbite@3.1.2/dist/flowbite.min.css" rel="stylesheet" />
  <script defer src="https://cdn.jsdelivr.net/npm/flowbite@3.1.2/dist/flowbite.min.js"></script>
</head>
<body class="bg-gray-100 dark:bg-gray-900 text-gray-800 dark:text-gray-100">
  <%- include ('header') %>
  <main class="flex items-center justify-center h-screen p-4">
    <div class="bg-white dark:bg-gray-800 rounded-xl shadow-lg p-8 w-full max-w-md">
      <h1 class="text-2xl font-bold mb-6 text-center">Choose Your Subscription</h1>
      <form action="/subscription/subscribe" method="POST" class="space-y-6">
        <div class="flex items-center justify-between border rounded-lg p-4 hover:shadow-md transition cursor-pointer">
          <label for="monthly" class="flex items-center space-x-3 w-full">
            <input type="radio" name="plan" value="monthly" id="monthly" class="accent-blue-600" checked />
            <div>
              <p class="font-semibold">Monthly Plan</p>
              <p class="text-sm text-gray-500 dark:text-gray-400">₹500 / month</p>
            </div>
          </label>
        </div>

        <div class="flex items-center justify-between border rounded-lg p-4 hover:shadow-md transition cursor-pointer">
          <label for="yearly" class="flex items-center space-x-3 w-full">
            <input type="radio" name="plan" value="yearly" id="yearly" class="accent-blue-600" />
            <div>
              <p class="font-semibold">Yearly Plan</p>
              <p class="text-sm text-gray-500 dark:text-gray-400">₹5000 / year</p>
            </div>
          </label>
        </div>

        <button type="submit" class="w-full bg-blue-600 text-white py-2 rounded-lg hover:bg-blue-700 transition">
          Proceed to Payment
        </button>
      </form>
    </div>
  </main>
  <%- include ('footer') %>
</body>
</html>