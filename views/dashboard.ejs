<!DOCTYPE html>
<html lang="en" class="dark">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Admin Dashboard</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://cdn.jsdelivr.net/npm/flowbite@3.1.2/dist/flowbite.min.css" rel="stylesheet" />
  <script defer src="https://cdn.jsdelivr.net/npm/flowbite@3.1.2/dist/flowbite.min.js"></script>
</head>
<body class="bg-gray-100 dark:bg-gray-900 text-gray-900 dark:text-gray-100 min-h-screen">
  <%- include('header', { user: user }) %>

  <div class="max-w-5xl mx-auto px-4 py-10 mt-10 mb-10">
    <h1 class="text-3xl font-bold mb-6">Admin Dashboard</h1>

    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
      <div class="bg-white dark:bg-gray-800 p-6 rounded-lg shadow">
        <h2 class="text-lg font-semibold">Total Users</h2>
        <p class="mt-2 text-2xl font-bold"><%= totalUsers %></p>
      </div>

      <div class="bg-white dark:bg-gray-800 p-6 rounded-lg shadow">
        <h2 class="text-lg font-semibold">Monthly Plan (Active)</h2>
        <p class="mt-2 text-2xl font-bold text-green-500"><%= monthlyActive %></p>
      </div>

      <div class="bg-white dark:bg-gray-800 p-6 rounded-lg shadow">
        <h2 class="text-lg font-semibold">Yearly Plan (Active)</h2>
        <p class="mt-2 text-2xl font-bold text-green-500"><%= yearlyActive %></p>
      </div>

      <div class="bg-white dark:bg-gray-800 p-6 rounded-lg shadow">
        <h2 class="text-lg font-semibold">Monthly Plan (Inactive)</h2>
        <p class="mt-2 text-2xl font-bold text-red-500"><%= monthlyInactive %></p>
      </div>
    </div>
  </div>

  <%- include('footer') %>
</body>
</html>
